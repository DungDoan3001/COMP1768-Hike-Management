<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hikemaui.Views.ObservationDetails"
             Title="Observation details">
    <StackLayout Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Text="Name: " FontSize="18" FontAttributes="Bold" Grid.Row="0"/>
            <Label Text="{Binding Name}" Grid.Row="1" FontSize="18"/>

            <Label Text="Time: " FontSize="18" FontAttributes="Bold" Grid.Row="2"/>
            <Label Text="{Binding Time}" Grid.Row="3" FontSize="18"/>

            <Label Text="Comments: " FontSize="18" FontAttributes="Bold" Grid.Row="4"/>
            <Label Text="{Binding Comment}" Grid.Row="5" FontSize="18"/>
        </Grid>
    </StackLayout>
</ContentPage>